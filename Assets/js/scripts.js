// scripts.js

document.addEventListener('DOMContentLoaded', function() {
    // Inicialización de AOS
    AOS.init({
        duration: 1000,  // Duración de la animación en milisegundos (opcional, ajusta según tus necesidades)
        once: true       // Animar elementos solo una vez mientras se desplaza
    });

    // Aquí puedes agregar cualquier otro código JavaScript que necesites para tu página

});
