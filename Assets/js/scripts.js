// scripts.js

document.addEventListener('DOMContentLoaded', function() {
    // Inicialización de AOS
    AOS.init({
        duration: 1000,  // Duración de la animación en milisegundos
        once: true       // Animar elementos solo una vez mientras se desplaza
    });

    

});
